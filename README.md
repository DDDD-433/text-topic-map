# Text Topic Map

Interactive topic map of BBC News articles — embeddings, topic modeling, and a Streamlit dashboard.

_Work in progress: full documentation lands after the pipeline and dashboard are built._

## Quick start

```bash
pip install -r requirements.txt
bash scripts/run_pipeline.sh
bash scripts/run_app.sh
```
